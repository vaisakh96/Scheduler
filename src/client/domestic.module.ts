import angular  from 'angular';
import domesCtrl from './domestic.controller'
import domesComponent from './domestic.component'

 export default angular
    .module('superman', [])
    .controller('domesCtrl', domesCtrl)
    .component('domesComponent', domesComponent)
    .name
